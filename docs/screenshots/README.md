Screenshots referenced by the root README.

Two files are expected here:

- `streamlit_ui.png`  — the main results view on the deployed Streamlit app,
  with a profile filled in and recommendation cards visible (match % + reasons).
- `fastapi_docs.png` — the Swagger UI at `http://localhost:8000/docs`,
  ideally with the `POST /api/v1/recommend` endpoint expanded.

Until both exist, the image embeds in README.md will render broken.
