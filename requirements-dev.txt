# Orbo Beauty AI — development-only dependencies
# Install with: pip install -r requirements-dev.txt
# These are kept OUT of requirements.txt so production deployments stay lean.

-r requirements.txt

# Testing
pytest==8.3.4
httpx==0.28.1          # required by FastAPI TestClient

# Dataset acquisition (only needed to rebuild data/processed/ from scratch;
# the committed parquet + joblib artefacts mean runtime never touches these)
datasets==5.0.1
huggingface-hub==1.28.0
kagglehub==1.0.2

# Config helpers
python-dotenv==1.0.1
pyyaml==6.0.3
